name: Echo Secret

on:
  workflow_call:
    secrets:
      RECEIVED_KEY:
        required: true

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Echo secrets
        run: |
          echo "received secret key: ${{ secrets.RECEIVED_KEY }}"
